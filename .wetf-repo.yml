---
# Repository configuration for wetransform managed repositories

# Reference presets that should apply to this repository
presets:
  - gradle-library
  - renovate
  - workflows-default

workflows:
  presets:
    - java-17
    - multi-module
    - skip-scan # currently not configured for scan
